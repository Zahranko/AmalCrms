import 'package:get/get.dart';

/// The notifications screen reuses the global NotificationCenterController
/// (registered permanently in main), so there's nothing to register here.
class NotificationsBinding extends Bindings {
  @override
  void dependencies() {}
}
